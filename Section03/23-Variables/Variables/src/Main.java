public class Main {
    public static void main(String[] args) {
        /*
        int           - is the variable type, in this case "int" (integer number)
        myFirstNumber - is the variable name
        =             - means that the value at it's right will be stored into the variable at it's left side
        5             - is the value to be stored
        */
        int myFirstNumber = (10 + 5) + (2 * 10); // <- it is an expression. A construct that evaluates to a single value
        int mySecondNumber = 12;
        int myThirdNumber = 6;

        System.out.println(myFirstNumber);
    }
}
